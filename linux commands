ps -ef
chmod +x filename
which python3
cat filename
pwd
echo
env :- it give the list of all the system defined variable
uname :- give the name of the operating system
sudo -i :- login through root user
$UID :- give the id of the current user. it is a system variable. root user always have 0 uid.